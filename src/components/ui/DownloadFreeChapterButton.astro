---
const { text, bgColor = 'white', textColor = 'black', noBorder } = Astro.props;
---

<a
  href="#free-chapter"
  style={`background-color: ${bgColor}; color: ${textColor}`}
  class={`bg-white ${!noBorder && 'border border-gray-100'} text-gray-900 px-6 py-3 rounded-md text-lg font-semibold shadow-md transition-all duration-200 transform hover:scale-105`}
>
  {text}
</a>
